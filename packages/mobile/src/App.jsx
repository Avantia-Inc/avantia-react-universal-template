import { createAppContainer } from 'react-navigation';

import BottomTabNavigator from './components/bottom-tab-navigator/BottomTabNavigator';

const App = createAppContainer(BottomTabNavigator);

export default App;
